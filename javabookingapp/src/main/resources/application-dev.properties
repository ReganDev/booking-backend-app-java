# ==========================================
# Development Configuration
# ==========================================

# Server
server.port=8080

# Database
spring.datasource.url=jdbc:postgresql://172.28.176.1:5434/bookingapp_dev
spring.datasource.username=postgres
spring.datasource.password=postgres512
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.baseline-version=1

# JWT Configuration
app.jwt.secret=dGhpcyBpcyBhIGRldiBzZWNyZXQga2V5IGZvciBib29raW5nIGFwcCB0aGF0IGlzIGF0IGxlYXN0IDI1NiBiaXRzIGxvbmc=
app.jwt.access-token-expiration-ms=900000
app.jwt.refresh-token-expiration-ms=604800000

# Logging
logging.level.org.springframework.security=DEBUG
logging.level.com.dev.bookingapp=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Enquiries / registration
app.registration-enabled=true
app.enquiry.to=graysitequeries@gmail.com
app.enquiry.from=${RESEND_FROM:onboarding@resend.dev}
app.resend.api-key=${RESEND_API_KEY:}
app.frontend-url=${APP_FRONTEND_URL:http://localhost:5173}
app.email-verification.expiry=${EMAIL_VERIFICATION_EXPIRY:24h}
app.email-verification.resend-interval=${EMAIL_VERIFICATION_RESEND_INTERVAL:60s}

# Platform admin (seeded on startup if no user with this email exists)
app.admin.email=admin@local.dev
app.admin.password=LocalAdmin123!
